# docker-compose.yml

services:
  api:
    build: .
    ports:
      - "8000:8000"   # porta_host:porta_container
    volumes:
      - ./instance:/app/instance   # il database persiste anche se riavvii il container